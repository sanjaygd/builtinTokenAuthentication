# builtinTokenAuthentication
Having feature of login, signup and built-in DRF token authentication. Built-in token authentication is based on session authentication which stores the token in database
